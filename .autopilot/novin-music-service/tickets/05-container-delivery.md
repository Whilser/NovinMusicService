# 05 — Docker-поставка, интеграция и документация

**Требования:** R02, R03, R20i, R22i, R24i
**Blocked by:** 02, 03, 04
**Зона:** `Dockerfile` · `docker-compose.yml` · `scripts/` · `README.md` · `.env.example` · `tests/integration/`
**Волна:** 4
**Status:** ready

## Что должно заработать

Готовая поставка для `novin`: образ с cifs-utils, безопасный entrypoint, Compose с постоянными данными и host-gateway, healthcheck, пример окружения, русская инструкция запуска и интеграционные проверки всех модулей.

## Из брифа, дословно

> «на сервере novin создать сервис в докере»
> «NAS шара доступна по SMB, нужно ее подмонтировать к образу в докере»
> «MPD (установлен на том же сервере)»

## Разделы спецификации

Истории 1–4, 25; Стек и поставка; Безопасность и ограничения.

## Критерии приёмки

- [ ] Dockerfile воспроизводимо собирает непривилегированное приложение с нужным mount helper и writable только `/data`, `/run` и `/music`
- [ ] Compose выдаёт минимально нужный `SYS_ADMIN`, запрещает лишние privileges, добавляет host-gateway, volume данных и healthcheck
- [ ] `.env.example` содержит пустые `SMB_USERNAME`, `SMB_PASSWORD`, `MPD_PASSWORD` и безопасные несекретные значения
- [ ] README по-русски объясняет build/up, первичную настройку, SMB guest/auth, MPD path mapping, backup и диагностику; предупреждает не публиковать порт в интернет
- [ ] Интеграционные тесты запускаются без NAS/MPD, проверяют API, персистентность и отсутствие секретов
- [ ] Полный тестовый прогон зелёный; Docker/Compose config проверены, если бинарники доступны

